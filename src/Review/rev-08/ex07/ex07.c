/* 
    C语言如何处理不同计算机系统中的不同文件和换行约定？
*/

/* 
    一、先理解“不同系统的换行约定”
        不同操作系统对“换行”的表示不一样（历史遗留问题）：

            Windows：用 \r\n（回车 + 换行，两个字节）表示一行结束。

            Unix/Linux/macOS：用 \n（仅换行，一个字节）表示一行结束。

            旧版 macOS（已淘汰）：用 \r（仅回车，一个字节）表示一行结束。

        如果直接把 Windows 写的文件拿到 Linux 读，或反过来，
        
        换行符不匹配就会导致显示错乱（比如多出奇怪符号）或解析错误（比如程序认为一行没结束）。
    
    二、C 语言如何“自动处理”跨系统换行？
        C 标准库通过 “文本模式”和“二进制模式”的文件打开方式，自动处理换行符转换：

        1. 文本模式（默认，不加 b）
            Windows 下：

                写文件时：把代码里的 \n自动转成 \r\n写入磁盘。

                读文件时：把磁盘里的 \r\n自动转成 \n读入内存。

            Unix/Linux/macOS 下：

                文本模式和二进制模式没区别（因为它们本来就只用 \n表示换行），读写时不会修改字节。

        2. 二进制模式（加 b，如 rbwb）
            无论哪个系统，所有字节原封不动地读写，不做任何转换。
    
    三、实操：怎么选模式处理文件？
        核心逻辑：文本文件用文本模式，二进制文件用二进制模式。
    
        场景 1：处理“文本文件”（如配置文件、日志、代码）
            用文本模式打开，让系统自动处理换行转换。
        
            // Windows/Linux/macOS 通用（文本模式，自动转换换行）
            FILE *fp = fopen("config.txt", "r");   // 读文本文件
            FILE *fp = fopen("log.txt", "w");     // 写文本文件
        场景 2：处理“二进制文件”（如图片、可执行文件、网络协议包）
            用二进制模式打开，避免系统偷偷修改字节（否则数据会损坏！）。
    
            // Windows/Linux/macOS 通用（二进制模式，原样读写）
            FILE *fp = fopen("image.jpg", "rb");  // 读图片（二进制）
            FILE *fp = fopen("program", "wb");    // 写可执行文件（二进制）
    四、注意事项（避坑关键）
        二进制文件必须用 b：
        
        如果二进制文件（如图片）用文本模式打开，Windows 会把 \n转成 \r\n，
        
            导致文件损坏（比如图片打不开、可执行程序崩溃）。
        
        文本文件用文本模式更省心：
        
            跨平台时，让系统自动转换单换行，不用自己手动替换 \r\n和 \n，减少错误。
        
            手动处理换行符时，要明确目标系统：
        
        如果需要自己拼接/解析换行（比如网络传输自定义协议），要根据目标系统转：
        
            发往 Windows → 把 \n转成 \r\n；
        
            发往 Unix/Linux/macOS → 直接用 \n。
        
            测试要覆盖多系统：
        
            在 Windows 写的文件，要在 Linux 下读；反之亦然。
            
            验证换行是否正常（比如用 cat -A 文件名在 Linux 下看是否有多余 ^M，
            
            或用记事本在 Windows 下看是否换行错乱）。
    
    五、举个“错误示范”和“正确示范”
        假设有个二进制文件（包含 \n字节），错误用文本模式写会怎样？        
        // 错误：二进制文件用文本模式（Windows 下会把 \n 转成 \r\n，数据损坏！）
            char bin_data[] = {0x0A, 0x0B, 0x0C}; // 包含 \n 的二进制数据
            FILE *fp = fopen("bin.dat", "w");    // 文本模式（默认）
            fwrite(bin_data, 1, 3, fp);          // 写入时，0x0A 会被转成 0x0D 0x0A！
            fclose(fp);
        
        正确做法是用二进制模式：
        // 正确：二进制文件用二进制模式，原样写入
            char bin_data[] = {0x0A, 0x0B, 0x0C};
            FILE *fp = fopen("bin.dat", "wb");   // 二进制模式（加 b）
            fwrite(bin_data, 1, 3, fp);          // 原封不动写入 0x0A 0x0B 0x0C
            fclose(fp);

    总结一句话
        C 处理跨系统文件和换行，核心是选对打开模式：
        
        文本文件用文本模式（自动转换换行），二进制文件用二进制模式（原样读写）。
        
        只要模式选对，系统会帮你自动处理换行差异，不用手动折腾字节～
*/